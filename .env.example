# Survival Test Tool — runtime configuration
# Copy this file to .env at the repo root and adjust values as needed.

# The origin your frontend is served from (VS Code Live Server default: 5500)
SURVIVAL_TOOL_FRONTEND_ORIGIN=http://127.0.0.1:5500

# The port the survival-test-tool server listens on
SURVIVAL_TOOL_PORT=8787